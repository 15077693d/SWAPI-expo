import { Link, Redirect, router } from "expo-router";
import {
  Button,
  ScrollView,
  Text,
  TextInput,
  View,
  Image,
  StyleSheet,
} from "react-native";

export default function Index() {
  return <Redirect href="/film" />;
}
